<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ '/styles/main.css' | url }}" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
</head>
<body>

  <img src="{{ '/assets/foto.jpg' | url }}" alt="Foto profesional" class="avatar" />

  <header>
    <h1><a href="{{ '/' | url }}">Verónica Mery</a></h1>
    <p>Futurismo suave, escritura creativa y tecnología personal</p>
    <nav>
      <a href="{{ '/' | url }}">Inicio</a> |
      <a href="{{ '/about/' | url }}">Sobre mí</a> |
      <a href="{{ '/contact/' | url }}">Contacto</a> |
      <a href="{{ '/posts/' | url }}">Publicaciones</a>
    </nav>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <p>© {{ "now" | date("yyyy") }} Verónica Mery</p>
  </footer>
</body>
</html>
